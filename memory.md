# WEB-MOJO Working Memory

## Memory Hygiene Rules
- Keep compact and current.
- Cap each section to 5 active bullets max.
- Prefer outcomes and decisions over narrative.
- Move completed items to Archive.

## Current Focus
- (empty)

## Key Decisions
- **Model+View pattern** — the primary record on a view is always `this.model`; JS reads via `this.model.get('field')`; templates read via `{{model.field}}`; child views receive `model: this.model`.
- **REST API — standard CRUD, no admin endpoints** — the permission system (User → Group → Member) handles access control per Model. All API access uses the same CRUD endpoints; admins filter with query params (e.g., `/api/account/api_keys?user=123`). Never create or assume separate admin-scoped endpoints like `/api/user/{id}/resource`.
- **Agent boot path** — `CLAUDE.md` → `memory.md`, then `/maestro-task` (file) / `/maestro-scope` (triage+plan) / `/maestro-build` (implement). Work items live on the maestro board, not in this repo.
- **Migrated to the maestro board (2026-07-26)** — the file-based planning pipeline is deleted: `planning/`, `scripts/{board,intake,start,close,ready}.sh`, the `/request` `/scope` `/build` skills, `WORKFLOW.md`, and `AI_DEV.md` are gone (git history preserves them). Work is now board-backed: workspace 17 (NativeMojo), board 11 (Backlog), project 14 (web-mojo) — resolved from `.claude/maestro.json`. Board 11 is **shared with django-mojo** (project 12), so `project: 14` must be set on every item filed from this repo. Stage is a column value (`inbox → scoped → planned → building → review → done`); the `## Plan` section in the description is still the build gate. Retained from the old workflow: green `npm test` baseline before first edit (`.claude/rules/build-baseline.md`) and **commit-on-finish** by explicit pathspec — never push, never branch (`.claude/rules/git.md`).
- **Docs source of truth** — local `docs/web-mojo/` is authoritative when inside this repo; never rely on `docs/pending_update/`.
- **Consumer agent file** — `docs/web-mojo/AGENT.md` is a drop-in for downstream projects and is intentionally separate from the internal `AGENT.md`.
- **Contributor guide** — `DEV_GUIDE.md` is contributor-facing and not part of the default agent boot path.
- **Chrome UI testing** — use `find` + `computer left_click` (real mouse clicks) for UI interaction testing. Never use `.click()` via `javascript_tool` — it bypasses the event pipeline and causes 404s on `<a>` tags. Use `javascript_tool` only for DOM assertions. Full protocol in `prompts/testing.md`.
- **View modules: trailing `export default X;`, not inline** — the test harness's `SimpleModuleLoader` only transforms a trailing `export default Name;` (like `DetailView`/`JobDetailsView`); an inline `export default class X extends View {…}` loads as `undefined`, so unit tests can't `Object.create(Cls.prototype)`. Use the trailing form for any view you want behavioral (non-source-text) tests on. (WM-015)
- **UI permission gating: `View#checkPermissions`, fail-closed** — `permissions:` keys on toolbar buttons and TableRow context-menu items gate through `View#checkPermissions` → `getApp()?.activeUser?.hasPermission()` (any-of for arrays); no resolvable user ⇒ hidden. Row menu items also support `visible(model)` (throwing predicate = hidden + warn) and callback `action(model, app)` (dispatched by original index via `data-menu-index`); `rowContextMenu` is an alias for `contextMenu` on TableView/TablePage. Escape any dev-supplied string flowing into template HTML with `escapeHtml` — and `escapeHtml` implementations must be explicit-replace (quote-safe), NOT div.textContent/innerHTML, because they're used in attribute contexts. (WM-020)
- **`admin` permission semantics** — User-record `admin` is the system-wide full-access wildcard (superuser-equivalent; name-checked in `hasPermission()`, deliberately **not** in `CATEGORY_GRANULAR_MAP`); surfaced as **"System Admin"** leading `User.CATEGORY_PERMISSIONS`. Member-record `admin` (**"Group Admin"**, leads `Member.BASE_PERMISSIONS`) is full access within that group only, never `sys.*`. `manage_group` (**"Manage Group"**) is a literal group-management perm, not an admin grant. Three distinct things: `admin` (system), member `admin` (group), `view_admin` (admin-panel entry). (WM-019)
- **`skipRender` model-change option** — `model.set(data, null, { skipRender: true })` / `model.save(data, { skipRender: true })` emit `change` normally but suppress the automatic `View` rerender (options are forwarded to listeners: `emit('change', model, options)`). FormView inline autosave uses it so saving one field doesn't rebuild parent views (was resetting tab state). Any view that hand-rolls its own `model.on('change', …)` rerender listener (e.g. DataView) must check the flag itself. (WM-016)
- **`epoch` formatter is format-agnostic** — the backend toggles timestamp serialization between epoch seconds and ISO-8601 strings, so `epoch` (DataFormatter) only `×1000`s *entirely-numeric* values; ISO/date strings, `Date` instances, and null/undefined/'' pass through unchanged for the downstream date formatter (`normalizeEpoch()` parses ISO via `Date.parse`). **Keep piping `epoch|datetime` / `epoch|relative` everywhere — never strip `epoch|` to "fix" an ISO field; fix belongs in the formatter.** Was `parseFloat('2026-…')`→2026→garbage date. (WM-021)
- **Page lifecycle traps** — (a) a Page `onEnter` override MUST `await super.onEnter()` (base sets `isActive`/clears `_wasExited`; skipping it makes `render()` a no-op on every revisit → blank page); (b) never `await` a fetch in `onEnter` — `WebApp.showPage` renders only after `onEnter` returns, so the page stays blank until the API answers; fire-and-forget + apply when children exist (stash + `onAfterMount` on first visit). (WM-023)
- **Pipes on empty arrays were broken framework-wide (fixed 2026-07-08)** — `dataFormatter.apply()`'s alternate-shape detection (`apply(value, [...pipeNames])`) misfired on `[]` (vacuous `every()`), returning the formatter NAME string (truthy) for ANY pipe on an empty array — flipping both `{{#x|bool}}` and `{{^x|bool}}`. Fixed with a non-empty guard + regression tests (`test/unit/Mustache-piped-sections.test.js`); both piped section forms now match Templates.md. Also: FormBuilder `json` fields return a parsed OBJECT from `getFormData()` when content is valid JSON (string when invalid) — coerce both (`geofenceData.coerceRuleInput`). (WM-023)
- **App-wide upload ceiling: `max_upload_size` (bytes, snake_case) in WebApp config** — resolved by `FileDropMixin.resolveMaxUploadSize(explicit, fallback)`: first positive finite number of page `maxFileSize` option → `app.config.max_upload_size` → surface fallback (storage Files page: 1 GB; 10 MB mixin baseline if every candidate is invalid). Storage-page-only by scoped decision — ChatInputView / FormView image fields / ImageUploadView stay at 10 MB. App-config keys follow the `shortlink_base_url` snake_case precedent. Loader gotcha: the test harness's `export default` transform also mangles COMMENTS containing that token pair — keep the phrase out of comments in harness-loadable modules. (WM-024)
- **Group `metadata` writes are deep-merged (objict.merge_dicts)** — `null` DELETES a key, lists overwrite, nested `__replace` is NOT supported (it would be stored + fail validation). To "replace" a nested config object (e.g. `metadata.geofence`), send a null-diff: new values + explicit `null` for every removed key/operator (`geofenceData.buildGroupRulePayload`). (WM-023)

- **ApiKey permissions = Member catalog, dotted-key switches (WM-025)** — an API key "acts as" a group member: its permissions editor offers exactly `Member.PERMISSION_TABSET`, embedded LIVE via a `fields` **getter** in `ApiKeyForms.create` (`rebuildPermissions()` replaces the tabset element in place, so a module-load spread captures a stale object). Saves are flat dotted `permissions.<name>` booleans (django-mojo expands pre-gate, per-key `can_change_permission`); creates are grant-only — strip falsy `permissions.*` keys before POST. Modal-layout trap: `noBodyPadding: true` + `buttons: []` are valid ONLY for `DetailView` bodies (they bring their own padding + × close); on a hand-rolled `View` those options remove all padding and every dismiss control — exactly how the old ApiKeyView modal looked broken. `is_active` belongs on the DetailView header active switch, not in edit forms. (WM-025)

- **DetailView kebab gating + admin send-flow endpoints (WM-027)** — `ContextMenu.visibleItems()` enforces `permissions:` (fail-closed via `View#checkPermissions`) and `when: (model) => bool` (re-evaluated every render); before WM-027 `permissions` on DetailView kebab items was silently ignored (ModalView's filter never runs for `header: false` detail modals). Admin send flows: reset initiates via `POST /api/auth/forgot` `{email|phone_number, method: link|code, channel?: sms}` (no SMS-link variant exists), magic link via `POST /api/auth/magic/send`; `revoke_sessions` / `send_invite` / `disable_totp` are `POST /api/user/<id>` POST_SAVE_ACTION bodies (never nested routes). **Path trap:** admin-targetable email verification is `/api/auth/email/verify/send` (public, `{email}`) — NOT `/api/auth/verify/email/send` (self-only); phone-verify send is genuinely self-only. Impersonation has NO backend route — cross-repo pair filed (django-mojo `admin-impersonation-handoff-code` + web-mojo `userview-impersonate-view-as-user`). (WM-027)

- **Geofence perms registered; `bypass_geofence` must never be category-implied (WM-029)** — `view_geofence` / `manage_geofence` / `bypass_geofence` live in the Platform tab of `User.GRANULAR_PERMISSION_TABS` (registry-only change — Sys Perms section + Edit Permissions modal rebuild from it, zero view code). **Deliberately no `CATEGORY_GRANULAR_MAP` entry for `bypass_geofence`** — it's a full geofence exemption (superuser-equivalent in scope for geo checks) and must be granted explicitly; a regression test pins this (`User.test.js` "no category permission implies bypass_geofence"). view/manage need no mapping either — geofence page gates already accept `sys.security`. Backend caveat baked into the switch tooltip: bypass only covers **authenticated** requests until django-mojo's post-login-geofence lands (login endpoints geofence pre-auth); update the tooltip when that ships. Deferred by scoped decision: AdminSecuritySection "Whitelisted from geofencing" discoverability row. (WM-029)

- **HTML escapers: `View.escapeHtml` is now quote-safe; use inherited `this.escapeHtml` for attribute-context HTML (WM-031)** — five self-defined escapers were `div.textContent`/`innerHTML`-based (escape `& < >` but NOT quotes → unsafe interpolated into attributes): `View`, `FormBuilder`, `TagInput`, `ComboInput`, `ChatInputView`. All switched to explicit-replace `& < > " '` (the form `TableRow`/`WebApp`/`DataFormatter` already used; `MOJOUtils` + `mustache.js` are quote-safe **and** aggressive — also escape `/` `` ` `` `=`). Fixing `View.escapeHtml` transitively hardens every inheritor that escapes into attributes (`ListView` `data-format`, TabView, SideNavView, ToastService, MetricCard, …). `ContextMenu.buildMenuItemHTML`/`renderTemplate` + `ModalView.buildContextMenu` now escape all dev-supplied item **and** trigger (`icon`/`buttonClass`) strings via `this.escapeHtml`. Escaping `data-action`/`data-*` is dispatch-safe — the browser decodes entities on `getAttribute()`. **NOTE:** WM-020's claim that TableRow uses a quote-safe escaper is CORRECT (its own explicit-replace at TableRow.js:843) — do not "fix" it. Still open (filed follow-ups): ModalView `buildFooter` button fields + modal `title` (same unescaped-config class); link-item `href` **scheme** sanitization (`javascript:`/`data:`). (WM-031)

- **dnsman admin: capability-driven, and three gates that look wrong but aren't (#394)** — the `system/dns/*` pages read `GET /api/dnsman/config` (django-mojo ≥ v1.2.55) through ONE cached helper, `registrar.capabilities()` in `models/Dns.js`; no view carries a version check. Server-owned values that must never be hardcoded client-side: the **provider matrix** (`providers[].purchase` decides management-only — not `provider !== 'route53'`), `allowed_record_types`, `max_domain_price` (arrives as `str(Decimal)`), `cert_renew_days` (drives `certExpiryTone`, so a retuned renewal scan can't silently disagree with the UI), and `acme.{configured,staging}` — **staging certificates are not publicly trusted and nothing else in the UI would say so**. Three backend gates the REST docs under-state: WHOIS **read** is `SAVE_PERMS` (registrar returns registrant PII regardless of privacy) so the section needs `manage_dns`; `registrar/adopt` checks the literal `is_superuser` attribute, NOT `hasPermission('admin')` which is broader; certificate material is never exposed by the UI at any level. **DNS record semantics:** `record_values` is always a list and an upsert REPLACES the whole set — hence `diffRecordValues` before any write, with the removal *warning* gated on a net shrink (warning on a same-size swap trains people to click through the one that matters). Route53 re-quotes and 255-chunks TXT itself, so user-added quotes must be stripped or SPF/DKIM/ACME break **silently**. A dotted relative name (`_sip._tcp`) hits the backend's out-of-zone refusal because any dotted name is treated as an FQDN (deliberate — otherwise `www.attacker.com` would be re-suffixed); our error suggests the in-zone form. All of this lives in pure `dnsData.js` and is unit-tested there.
- **Registrant Contact page: the house scope is `is_superuser`, and `_raw` is scope-bound (#952)** — `system/dns/registrant` edits the ICANN contact in two scopes. **The house scope gates on the LITERAL `activeUser.get('is_superuser')`**, because django-mojo's `require_platform_admin` (`dnsman/rest/gates.py:47`) checks that attribute — not `manage_dns`, not the `admin` wildcard, which is broader; gating on a permission renders a control that 403s. Exactly the `DomainTablePage` Adopt-button precedent. **Do not probe the endpoint to discover the answer**: those refusals carry `branch="dnsman.platform_admin.not_superuser"` and `gates.py` says those "are the ones worth alerting on", so probing alarms on every non-superuser page load. **`buildContactPayload(raw, form)`'s `raw` must belong to the scope being saved, or be null** — it carries `Fax`/`ExtraParams` across a save (the backend REPLACES rather than merges, so dropping them destroys ccTLD registry params) but carrying it across a *scope change* writes the house contact's `ExtraParams` — AWS's slot for date of birth and national ID — into a tenant's row, invisibly, since neither key is rendered. Cleared on every scope change and only re-seeded from a `source: "database"` load of the current scope. An inherited scope shows neither values nor `problems` (a problem list tells a tenant which fields of the *house* contact are broken), and the banner must not claim *which* contact is inherited — `Setting.resolve()` walks the parent-group chain before the global row. `group` is **omitted, never null**, from a POST body (the backend guard is `"group" in request.DATA`). `registrar.capabilities()` is now group-keyed; the no-arg form still means deployment-wide. **Framework bug found and filed, not fixed:** a FormView `buttongroup` cannot drive `showWhen` — `onActionSelectButtonOption` never calls `handleFieldChange`, which is `_updateShowWhen`'s only caller — so a dependent toggled by one renders hidden and stays hidden; it also emits `field:changed` where the rest of the form emits `field:change`.
- **Source-text test assertions pin PROSE, not code (#394)** — three suites in one build failed on their own documentation: a file that explains in a comment why it never touches `localStorage` / `certificate/material` / a reveal control trips a naive `not.toContain` grep. Strip comments before asserting (`stripComments` helper at the top of `DnsViews.test.js` / `DomainPurchaseWizard.test.js`), target the CONTROL not the word (`action: '…download'`, not `download` — the view legitimately contains the sentence "not downloadable from this panel"), and slice method BODIES via a `methodBody(name)` helper matching `name() {` — `indexOf('runPurchase()')` lands on the method's own signature and `indexOf('renderConfirm()')` on a call site in a switch.

## In-Progress Work
- (empty)

- **Four `table:*`/`filter:edit` events never existed; docs follow code (#298)** — `table:search`, `table:page`, `table:pagesize` and `filter:edit` were documented on TableView/TablePage but appear nowhere as an `emit()` in `src/`. **Deleted rather than aliased** — three lines would have made them real, but nobody can be successfully using names that never fired, and an alias doubles the event surface forever. The toolbar lives on **ListView**, so a TableView emits `list:search` / `list:page` / `list:pagesize` / `list:show-more` / `list:sort` / `list:add` / `list:export`; `table:*` is only `add` / `export` / `sort` / `fullscreen:enter` / `fullscreen:exit`. **`params-changed` is the umbrella** — use it for URL sync instead of any individual event. `cell:save:error` is now forwarded by `TableView._createItemView` (was row-only, so the error branch was unreachable from the table). Three trap notes for anyone touching this area: **(a)** `isSelectable()` = `batchActions.length > 0 && selectionMode === 'multiple'`, so `selectable: true` alone renders **no** checkbox column; **(b)** `column.width` is inert — nothing reads it; **(c)** `searchPlacement: 'dropdown'` was documented but unimplemented — since fixed in #494. Two live inline-edit defects found and filed, not fixed: `exitEditMode` runs `escapeHtml(pipe(value, formatter))` so an HTML formatter (`badge:`) repaints as literal markup after a save (the initial render is triple-brace/unescaped — the two paths disagree), and `cell:save:error` only fires when `model.save()` **rejects**, which stock `Model.save()` never does (it catches and returns `{success:false}`). **Verification note that generalizes:** the three `examples-*` build suites check a route's *registration* only — registry checks `sourcePath` exists, imports is pure regex, coverage is folder-level — so **an example that throws on mount passes `npm test` clean**. Examples must be loaded in a browser. `examples:registry` now also runs the previously-orphaned `cross-link-docs.js`.

- **`Model.save()` NEVER rejects — inspect the resolved response (#495)** — it catches transport errors and resolves `{success:false}`, and on a `{status:false}` body it stores the body in `model.errors` and still resolves `success:true`. Anything awaiting `model.save()` and relying on `try/catch` alone will treat a **server-side validation failure as a success** — that was silent data loss in inline cell editing for as long as the feature existed. The rule that generalizes: **failure ⟺ rejected OR `resp.success === false` OR `model.errors` non-empty**, and prefer `model.errors` over re-testing `resp.data.status` so the verdict stays delegated to `Model` rather than duplicated (it also keeps `{success:true}`-with-no-`data` fakes working). Second half of the same item: **TableRow's post-edit repaint must not escape.** Every branch of `buildCellTemplate` is already unescaped — `{{{model.key|fmt}}}`, `innerHTML = formatter(...)` for function formatters, and `{{{model.key}}}` for a bare value — so escaping only in the repaint protected nothing (the next full render re-emits it unescaped, and a REST save triggers one via `set()`) and merely made HTML formatters show as literal markup after an edit. Both paints now go through one `renderCellContent()`. **Open/filed:** those `{{{ }}}` mean every TableRow cell emits raw model values unescaped — a genuine framework-wide XSS surface whose fix would change behavior for every existing table.
- **Option gates must fail SAFE, not fail closed (#494)** — `searchPlacement: 'dropdown'` was documented for a long time with no renderer because the gate read `searchable && searchPlacement === 'toolbar'`. That shape is the real bug: *any* unrecognized value (a typo like `'Toolbar'` as much as `'dropdown'`) silently removed the search input with no console signal. **When an option selects between render paths, normalize it once in the constructor and fall back to the default with a `console.warn` — never let an unknown value select "render nothing".** Now `_normalizeSearchPlacement()`; `TableView` must NOT re-assign `searchPlacement` after `super()` or it bypasses the normalizer. Two reusable specifics from the dropdown build: a Bootstrap `dropdown-menu` containing an `<input>` needs **`data-bs-auto-close="outside"`** on the toggle or the menu closes on the first click inside (precedent: `MultiSelectDropdown.js:161`); and a control that *collapses* an applied filter needs an active-state cue on the collapsed trigger (`{{#searchValue}}active{{/searchValue}}`) — driven by a Mustache section, because `this.searchValue` is refreshed per render while the template string is built once.
- **`stats:` live stat strip (#300, WM-038 epic)** — opt-in KPI blocks above the toolbar; counts come from ONE batched `GET <list endpoint>?…&_mode=count&_stats={key:params}` (cap 12 = `MOJO_REST_AGG_STATS_CAP`). **The response envelope is FLAT — `resp.data.{count,stats,took_ms}`, NOT `resp.data.data`** — that path returns `JsonResponse(body)` directly instead of `response.success(data)`, so `.claude/rules/api.md`'s "commonly at `.data.data`" does **not** apply; reading it wrong yields a permanently-degraded strip that still passes a test written on the same wrong assumption. An **old server does not error** on `_mode`/`_stats` (`build_rest_filters` has no else branch — unknown keys are silently dropped), it answers HTTP 200 with an ordinary list page and no `stats` key: hence the **absent-key latch** (`_statsSupported = false`), without which every filter change would pull a full row page forever. Base params for the counts **exclude the ACTIVE bundle's keys** — the server ANDs bundles onto the base while the UI applies them with mutual exclusion, so leaving them in would advertise `open AND high` for a click yielding `high` alone — and the exclusion must mirror **both** `setFilter` branches (the `dr_start`/`dr_end`/`dr_field` daterange triple as well as `key`/`field`/`field__in`), because `_clearPresetParams` goes through `setFilter`. Consequence worth knowing: base params are **invariant across stat clicks**, so switching stats issues no recount at all. `params-changed` is **not** "filters changed" — 4 of its 8 emit sites are page/page-size/sort, which mutate exactly the keys the count request strips, so a signature memo suppresses them (auto-refresh ticks bypass it, both modes). Seed the first count from `onAfterMount()`, never `onInit()` (pre-mount → the `isMounted()` guard kills it). Style note: the strip must **not** emit `scrollbar-width: thin` — the preset segment owns the only one and `ListView.interactions.test.js` pins that count at 1.
- **`autoRefresh` object form + models mode + row flash (#297, WM-038 epic)** — `autoRefresh` takes `{ every, mode, indicator, flash }`; the key is **`every`** (seconds), never `interval` (every `*Interval` in this repo is ms). `mode:'models'` refreshes only the visible rows through the new `Collection.refreshModels(ids)` → `{changed:Map, missing, ok}`. It must **bypass `collection.fetch()`**: fetch aborts an in-flight request on a differing param key, emits `fetch:start` (which swaps the list for the skeleton — so "silent refetch" was never visually silent), and `add()`s new ids. It also never calls `parse()` (that rewrites `meta` → breaks "Showing N of M"), overrides `start`/`size` per chunk (a `fetchMore`-grown list would truncate), and **diffs before set** reading `m.attributes[k]` (not `get()`, which splits on `.`/`|`) with sorted-key compare for objects — `_setNestedAttribute` uses `!==`, so identity alone would flash the whole table every tick. The flash is **models-only by construction** (a collection reset → `_buildItems()` → `_clearItems()` destroys every item view, so no row survives) and paints **`background-image`**, because `rowStripe` owns `box-shadow: inset 4px 0 0` on `td:first-child` and keyframes outrank normal author declarations — verified live in both themes: the severity bar stays lit through the flash. Stale-response guard = `lastFetchTime` snapshot (the view's own AbortController is invisible to `Collection.fetch`). TablePage unchanged — the object form rides the existing key by reference.
- **ListView/TableView opt-in feature suite + composition contracts (WM-038 epic, 2026-07-18)** — six table features shipped strictly opt-in (`filterPresets`, `emptyState`/`loadingStyle:'skeleton'`/`showResultCount`, `autoRefresh`, `persistState`+`persistKey`, `columnChooser`, `rowExpand`+`rowExpandMultiple`); each MUST appear in TablePage's forwarding whitelist + `TablePage.option-forwarding.test.js`. Composition contracts (locked by `test/unit/ListView.interactions.test.js`): every toolbar sub-feature repaints ONLY its own `data-container` (never a shared parent's innerHTML); style blocks gated per-feature, emitted once; all-options-off render is byte-identical to baseline. Preset active state is DERIVED from `getActiveFilters()` matching (no stored flag); persistState precedence is URL > saved > defaults (pre-init param snapshot) and NEVER persists `expandedRows`. Ian's design language: compact grouped segments (SegmentControl family, no bubbly pills), flat monochrome stat/KPI elements, icon-only toolbar buttons (label `d-none d-xxl-inline`). WM-037 stat strip trails on django-mojo `table-stats-aggregation`; approved Variant B design recorded in its item. Framework seam found (WM-034): cached-page `unmount()` does NOT fire child `onBeforeUnmount` — child timers need idempotent-start + self-terminating-tick patterns.

- **Edge template kinds (#1620, django-mojo ≥ 1.6.0)** — vhost kinds are `api|site|site_api|redirect` and `VHOST_KIND_MATRIX` in `models/Edge.js` is the single source for payload builder, wizard, edit form, and detail rows (off-matrix knobs are SENT as neutral values so stale state clears on update). Creation is `VhostCreateWizard` (hand-rolled View + `Modal.show(w, {title:false,size:'lg',buttons:[]})`; no kind dropdown anywhere; kind immutable in the UI — change shape = delete+recreate, with a linked-WebApp warning because `WebApp.vhost` is SET_NULL and the web root derives from `Vhost.pk`). **site_api cannot carry quiet_paths at create** (server checks coverage against declared routes, which need the vhost FK) — wizard creates vhost → routes sequentially, quiet paths live in the edit form. **Duplicate (domain,label)/(kind,value) writes return 500 "system error"** (DB unique constraint, no validator dup check — dj#1621): the wizard pre-checks by comparing `server_name` client-side (apex label is `''` and can't travel as a query param); blocklist form carries honest failure copy. Blocklist admin (`system/edge/blocklist`) gates ALL controls `sys.`-prefixed (geofencing precedent — backend counts global security grants only). Loader: `EdgeModelsStub`/`VhostFormStub` mappings + `VhostCreateWizard` registry entry; wizard tests mock at the model-class prototype seam (loader-cached Model/Collection are bound to the real Rest).

## Lint Baseline
- `npm run lint` = `eslint src` (fixed 2026-07-08; the old `src/**/*.js` glob never recursed under /bin/sh — extensions were NEVER linted). Baseline: **0 errors / 316 warnings**. Warnings are tracked debt: 4 recommended rules downgraded to warn (`no-useless-escape`, `no-case-declarations`, `no-prototype-builtins`, `no-empty`); intentional lazy `import()` sites carry inline eslint-disable comments; `bootstrap` is a declared global; generated `countryCentroids.js` is ignored (its BQ/ES/TF duplicate keys are a generator artifact).
- **Unit-testing a heavy DetailView-based view — loader import mechanics (WM-026)** — `simple-module-loader` transforms imports at load: a **default** import whose path maps in `importPathToGlobal` → `const X = global.X` (captured at load, so set the global BEFORE `loadModule`; you may then `delete` it — the module keeps its captured ref); an **unmapped default** import → a constructible module-local stub; but an **unmapped named** import (`import { LogList }`) → `const X = undefined`, so a constructor-time `new X()` throws. A view that `new`s a model/collection in its constructor therefore needs that model's path in `importPathToGlobal` (added `models/Log.js → LogList`) and the view registered in the loader. Never leave a `global.<RealModule>` assigned — it leaks into later test files (once broke a `ShortLinkView` `instanceof MetricCard` assertion via loadModule's cache-hit not restoring the global); capture-then-`delete`, or don't touch it. Prefer construction-only assertions (read `headerConfig`/`sectionsConfig`, drive action handlers directly) over full `render()`.

## Open Questions
- (empty)

## Archive
- WM-026 (2026-07-12) MemberView: Audit section gate widened to the server LogList VIEW_PERMS `[manage_logs, view_logs, security, admin]` (was bare `view_logs` — fail-closed but locked out `security`/`manage_logs`-only operators); added an "Edit membership" kebab action (role-only via `showModelForm`; `is_active`=header switch, `display_name`=UserView; shared `Member.EDIT_FORM` untouched). +`MemberView.test.js` (6 tests) + loader registration/`Log.js`→`LogList` mapping. Suite 1331/1331, lint 0 errors. Surfaced by the MojoVerify portal adopting the framework MemberView (portal#ITEM-017). Unreleased (version bump pending publish).
- `RunnerDetailsView.js` was refactored from `this.runner` to `this.model` to match framework patterns.
- Agent context restructure completed: `AGENT.md`, `docs/agent/architecture.md`, `memory.md`, `prompts/building.md`, `prompts/planning.md`.
- MultiSelectDropdown + Django lookups filter system shipped.
- Auth, Wiki, Cloud Push Notifications, Metrics permissions, and Firebase support shipped.
- Chart rendering flow and Series Chart refresh button fixed.