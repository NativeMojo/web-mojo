/**
 * MOJO Admin Models — Entry (`web-mojo/admin-models`)
 *
 * Data-only entry: 14 admin-coupled Model/Collection classes with no UI deps.
 * Use this when you want the models for an API client, a Node script, or a
 * different UI library — anything that doesn't need the admin pages from
 * `web-mojo/admin`.
 *
 * For the admin UI (sidebar pages, tables, views) use `web-mojo/admin`.
 */

export * from '@ext/admin/models/index.js';
