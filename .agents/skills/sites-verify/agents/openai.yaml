interface:
  display_name: "Sites Verify"
  short_description: "Visually verify a deployed Maestro site"
  default_prompt: "Use $sites-verify to inspect the deployed site in real desktop and narrow-view renders."
dependencies:
  tools:
    - type: "mcp"
      value: "maestro"
      description: "Read and update the shared Maestro workspace and boards"
      transport: "streamable_http"
      url: "https://maestromojo.com/mcp"
