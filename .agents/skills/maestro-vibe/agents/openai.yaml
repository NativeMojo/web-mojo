interface:
  display_name: "Maestro Vibe"
  short_description: "Ship a small change with lightweight tracking"
  default_prompt: "Use $maestro-vibe to implement and verify this small change, then record it on Maestro."
dependencies:
  tools:
    - type: "mcp"
      value: "maestro"
      description: "Read and update the shared Maestro workspace and boards"
      transport: "streamable_http"
      url: "https://maestromojo.com/mcp"
